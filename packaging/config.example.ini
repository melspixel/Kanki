# Kanki sync configuration. The application never logs the hkey value.
# Existing RAnki users can leave this file absent: kanki-sync.sh will migrate
# hkey and endpoint from /mnt/us/extensions/ranki/config.ini on first sync.
hkey=
endpoint=
